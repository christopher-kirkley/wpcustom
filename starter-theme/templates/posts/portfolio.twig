{% extends 'base.twig' %}

{% block content %}
	<div>
		<div>
			<img src="{{ post.thumbnail.src('large') }}" width="50%" alt="">
			<h2>{{ post.title }}</h2>
			<div>
				{{ post.content }}
			</div>
		</div>
	</div>
{% endblock %}
